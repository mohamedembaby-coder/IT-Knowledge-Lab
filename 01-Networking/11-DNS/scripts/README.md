# DNS Evidence Scripts | أدوات جمع دليل DNS

- `get-dns-client-evidence.ps1`: gathers local Windows DNS client settings and selected lookup results.
- `capture-dns.sh`: starts a bounded DNS packet capture for an interface.

Run only with authorization. Review output before sharing because hostnames, DNS suffixes, and IP addresses may be sensitive.
